[package]
name = "ctsverifier_jni"
version = "0.1.0"
edition = "2024"
authors = ["The AOSP and FrizkOS Authors"]
description = "Modern Rust implementation of CTS Verifier native JNI methods (FileUtils)"

[lib]
name = "ctsverifier_jni"
crate-type = ["cdylib", "rlib"]

[dependencies]
